{
  "name": "setec",
  "version": "0.1.0",
  "description": "SETEC — a class-based CSS style library with 1980s terminal aesthetic and modern wide-gamut OKLCH colors.",
  "style": "dist/setec.css",
  "scripts": {
    "build": "postcss src/index.css -o dist/setec.css && postcss src/index.css -o dist/setec.min.css --env production && cp dist/setec.css docs/setec.css",
    "watch": "postcss src/index.css -o dist/setec.css --watch"
  },
  "keywords": ["css", "terminal", "dark-theme", "oklch"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "cssnano": "^7.1.5",
    "postcss": "^8.5.10",
    "postcss-cli": "^11.0.1",
    "postcss-import": "^16.1.1",
    "postcss-nesting": "^14.0.0"
  }
}
